# SW-09: Final Audit

## Context
This is the final audit ticket for agent-swarm. All features are merged.

## Your Scope
1. Verify ALL CLI commands work end-to-end:
   - `swarm init testproject` → creates scaffold
   - `swarm status` → reads tracker, prints table
   - `swarm status --json` → valid JSON output
   - `swarm add-ticket test-1 --phase 1 --desc "test"` → adds to tracker
   - `swarm prompts check` → reports missing prompts
   - `swarm prompts gen test-1` → creates template
   - `swarm cleanup --older-than 0s` → prunes (no-op if nothing)

2. Run full test suite: `go test ./... -v -count=1`
   - Fix any failures
   - Ensure minimum 60% coverage on core packages

3. Verify build:
   - `go build -o swarm .` produces working binary
   - `go vet ./...` clean
   - Binary runs: `./swarm --help`

4. Create README.md:
   - Quick start, install (`go install`), all commands documented
   - Config reference (swarm.toml)
   - Architecture diagram (text)

5. Create .goreleaser.yml for release automation

6. Final commit: `feat(sw-09): final audit — all commands verified, README, release config`
