# TP-11: Migration command for legacy post-build scope

## Objective
Add migration from per-feature post-build deps to run-level post-build state.

## Depends
tp-10

## Files to touch
- cmd/migrate_cmd.go (or equivalent)
- internal/tracker/migrate/*
- tests

## Implementation Steps
1. Implement `agent-swarm migrate post-build-run-scope --dry-run|--apply`.
2. Detect legacy graph patterns.
3. Generate and optionally apply tracker transformation.
4. Produce readable diff summary.

## Verify
go test ./cmd/... ./internal/tracker/...

## Done Definition
- safe migration for existing projects with dry-run and apply
