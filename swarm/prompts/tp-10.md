# TP-10: Run-scope post-build idempotency model

## Objective
Implement run-level post-build execution once per run with idempotency.

## Depends
tst-tp2

## Files to touch
- internal/tracker models
- internal/dispatcher logic
- internal/watchdog done handlers
- status/API models

## Implementation Steps
1. Add run state: currentRunId + runs map + integration + postBuild statuses.
2. Add ticket types: feature/integration/post_build.
3. Dispatch order: features -> integration -> post_build chain.
4. Enforce idempotency key `<run_id>:<step>`.

## Verify
go test ./internal/dispatcher/... ./internal/watchdog/... ./internal/tracker/...

## Done Definition
- post-build standard steps execute once per run, never per feature
