# TP-01: Tracker schema v2 + validators

## Objective
Implement Ticket Schema V2 in tracker models with required implementation-ready fields and strict validation.

## Scope In
- internal/tracker/*.go
- tracker JSON load/save validation paths
- tests for schema validation

## Scope Out
- No dispatcher behavior changes yet
- No prompt compiler in this ticket

## Files to touch
- internal/tracker/tracker.go
- internal/tracker/validate.go (new)
- internal/tracker/*_test.go

## Implementation Steps
1. Extend ticket model with required fields from docs/SWARM-TICKET-PREP-V2-SPEC.md section 5.
2. Add validator that enforces required fields and role presence.
3. Ensure backward compatibility toggle for legacy tickets if needed via non-strict mode.
4. Add tests: valid v2 ticket, missing field failures, invalid arrays/commands.

## Verify
go test ./internal/tracker/...

## Done Definition
- tracker can parse/save schema v2 fields
- validation errors are structured and actionable
- tests pass

## Constraints
- No CLI command changes in this ticket
- Keep existing tracker behavior for unrelated fields intact
