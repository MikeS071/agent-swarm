# TP-08: Copy-on-spawn .agent-context + manifest

## Objective
Bind immutable role/rule context into each worktree at spawn time.

## Depends
tp-07, tp-03

## Files to touch
- internal/worktree/*
- internal/backend spawn path
- maybe internal/dispatcher hooks
- tests

## Implementation Steps
1. Materialize `.agent-context/` in worktree before spawn.
2. Copy resolved rules/profiles/roles/skills snapshot files.
3. Emit `context-manifest.json` with sha256 hashes and source paths.
4. Store manifest path in tracker events/log.

## Verify
go test ./internal/... 

## Done Definition
- spawned ticket has immutable context snapshot
- manifest is auditable and deterministic
