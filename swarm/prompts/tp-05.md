# TP-05: Add prep command + watch preflight gate

## Objective
Add `agent-swarm prep` command and block watch unless prep passes (unless explicit override).

## Depends
tst-tp1

## Files to touch
- cmd/prep_cmd.go (new)
- cmd/watch.go
- tests

## Implementation Steps
1. Implement `prep` pipeline: tickets lint -> prompts build --all -> prompts validate strict -> spawnability checks.
2. Update watch/watch --once to require prep success by default.
3. Add `--allow-unprepared` override with loud warning.

## Verify
go test ./cmd/... ./internal/...

## Done Definition
- watch is gated by prep by default
- prep returns actionable failure output
