# TP-12: TUI/API run-level post-build reporting

## Objective
Expose run-level progress and post-build suite status in TUI/status/API.

## Depends
tp-10

## Files to touch
- internal/tui/*
- internal/server/*
- cmd/status output
- tests

## Implementation Steps
1. Add run progress block separate from feature stats.
2. Show post-build step statuses (pending/running/done/failed/skipped).
3. Expose same data in JSON and API payloads.

## Verify
go test ./internal/tui/... ./internal/server/... ./cmd/...

## Done Definition
- operators can clearly see feature vs run-level post-build progress
