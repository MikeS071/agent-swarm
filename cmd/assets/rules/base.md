# Base Rules

- Keep changes scoped to the assigned ticket.
- Write tests before implementation and keep tests deterministic.
- Validate external inputs before executing business logic.
- Return explicit errors; do not hide failures.
- Do not hardcode secrets; use environment variables.
- Preserve idempotent behavior when scaffolding or migrations run multiple times.
